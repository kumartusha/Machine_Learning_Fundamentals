# Here’s a list of tasks for each library (NumPy, Pandas, Matplotlib, and Seaborn) to help you practice and enhance your understanding. Each task is designed to cover important concepts and functionalities.
# ***********************************************************************************************************************************
# NumPy Tasks (20 Questions)
# Create a 1D, 2D, and 3D array and print their shapes.
# Generate an array of 100 random numbers and find their mean, median, and standard deviation.
# Create a 4x4 identity matrix.
# Reshape a 1D array of size 12 into a 3x4 matrix.
# Perform element-wise addition, subtraction, and multiplication on two arrays.
# Slice a 5x5 matrix to extract the center 3x3 sub-matrix.
# Find the dot product and cross product of two vectors.
# Replace all even numbers in an array with -1.
# Stack two arrays vertically and horizontally.
# Create a boolean mask for elements greater than 10 in an array.
# Sort a 1D array in ascending order.
# Find the indices of the maximum and minimum values in an array.
# Generate a random array of integers between 1 and 100 and count the unique values.
# Perform matrix multiplication using np.dot.
# Create a diagonal matrix with specific values on the diagonal.
# Split an array into 3 equal parts.
# Find the determinant and inverse of a 2x2 matrix.
# Flatten a multi-dimensional array into a 1D array.
# Create a custom function and apply it to an array using np.vectorize.
# Generate a random array of 100 elements and clip the values to a range between 10 and 50.

# *********************************************************************************************************************************
# Pandas Tasks (20 Questions)
# Create a DataFrame from a dictionary.
# Read data from a CSV file into a DataFrame and display the first 5 rows.
# Add a new column to a DataFrame based on existing columns.
# Filter rows in a DataFrame where a specific column's value is greater than 50.
# Group data by a column and calculate the mean for each group.
# Replace missing values in a DataFrame with the column mean.
# Drop rows with any missing values.
# Rename columns in a DataFrame.
# Sort a DataFrame by a specific column in ascending and descending order.
# Merge two DataFrames on a common column.
# Create a pivot table from a DataFrame.
# Export a DataFrame to a CSV file.
# Count the number of unique values in a specific column.
# Apply a lambda function to transform a column.
# Create a DataFrame with datetime values and extract the year, month, and day.
# Concatenate two DataFrames vertically and horizontally.
# Calculate the correlation matrix for a DataFrame.
# Generate a histogram of a column’s values.
# Split a column containing full names into first and last names.
# Create a DataFrame from a NumPy array and add appropriate column names.

# ***********************************************************************************************************************************
# Matplotlib Tasks (20 Questions)
# Create a line plot of a sine wave using plt.plot.
# Add a title, xlabel, and ylabel to a line plot.
# Create a scatter plot of random x and y values.
# Generate a bar plot with labels on the x-axis and values on the y-axis.
# Plot a histogram for a random array of 1000 numbers.
# Create a subplot with 2 rows and 2 columns of plots.
# Plot multiple lines on the same graph with a legend.
# Customize a plot by changing line styles, colors, and markers.
# Save a plot as an image file (e.g., PNG).
# Create a pie chart with labels and percentages.
# Plot a heatmap using a 2D array.
# Add grid lines to a plot and customize their appearance.
# Display a mathematical equation as text on a plot.
# Annotate a specific point on a plot with an arrow.
# Adjust the size and resolution of a plot using figsize.
# Create a stacked bar chart.
# Use fill_between to shade the area under a curve.
# Plot a function over a specific range (e.g., y = x^2 for x in [0, 10]).
# Create a log-scale plot.
# Use twin axes to plot two different datasets with separate y-axes.

# *********************************************************************************************************************************
# Seaborn Tasks (20 Questions)
# Create a scatter plot using sns.scatterplot.
# Generate a line plot with a confidence interval using sns.lineplot.
# Create a bar plot to compare categories using sns.barplot.
# Visualize a count of categorical data using sns.countplot.
# Plot a distribution of data with a kernel density estimate using sns.distplot.
# Create a box plot to visualize the distribution of a numerical variable.
# Create a violin plot to visualize the distribution of a numerical variable.
# Generate a pair plot for a DataFrame.
# Create a heatmap of a correlation matrix.
# Customize the style of a plot using sns.set_style.
# Add a custom color palette to a Seaborn plot.
# Create a regression plot with sns.regplot.
# Use hue to create a scatter plot grouped by a category.
# Plot a swarm plot for a categorical variable.
# Combine a violin plot with a swarm plot.
# Display multiple plots in a FacetGrid.
# Use a joint plot to visualize two variables with marginal distributions.
# Create a categorical plot using sns.catplot.
# Add titles and labels to a Seaborn plot.
# Create a time series plot with Seaborn.